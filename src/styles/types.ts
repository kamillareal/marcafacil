export interface ThemeProps {
  palette: {
    common: {
      white: string;
      black: string;
      main: string;
    };
    primary: {
      dark: string;
      main: string;
      light: string;
    };
    secondary: {
      dark: string;
      main: string;
      light: string;
    };
  };
}
